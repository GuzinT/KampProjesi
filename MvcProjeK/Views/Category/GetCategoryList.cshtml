@using EntityLayer.Concrete 
@model List<Category>

@*html sayfasının içerisine concrete dizinini dahil ettim
ve heemn bir model oluşturduk. model;verileri listelemek için kullanacağımız bileşen. bu model değer olarak categorynin listesini tutacak
c# komutlarını kullanabilmek için ilgili ifadenin başına @ sembolü koymak gerekiyor.Bunun adı burada Razor Syntax olarak adlandırılıyor.*@

@{
    Layout = null;
}
<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<br />
<table class="table table-bordered">
    <tr>
        <th>ID</th>
        <th>Ad</th>
        <th>Durum</th>
    </tr>
    @foreach(var item in Model)
    {
        <tr>
            <td>@item.CategoryID </td>
            <td>@item.CategoryName</td>
            <td>@item.CategoryStatus</td>
        </tr>
    }

</table>
<br />
<a href="/Category/AddCategory" class="btn btn-info">Yeni Kategori</a>

